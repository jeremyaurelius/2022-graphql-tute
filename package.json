{
  "name": "book-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:react-client": "npm run --prefix react-client start",
    "start:ng-client": "npm run --prefix ng-client start",
    "start:server": "node server/app",
    "watch:server": "npx nodemon server/app",
    "start:server:mem-storage": "node server/app --storage=mem",
    "watch:server:mem-storage": "npx nodemon server/app --storage=mem"
  },
  "author": "",
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
